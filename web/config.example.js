// SKOGARK web config — OPTIONAL.
//
// Copy this file to `config.js` and set the URL of your deployed AIS proxy
// (see server/README.md). When set, the riverboat tour gains a "🚢 Ships"
// button that opens a live map of vessels on the Savannah River, and Captain
// Mike names a real nearby ship as you pass the port.
//
// Leave it unset (or don't create config.js) and the game runs exactly as
// before — the ships feature simply stays hidden.

window.SKOGARK_VESSEL_API = "https://your-proxy-host.example.com";
